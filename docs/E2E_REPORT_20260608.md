# E2E 実行レポート（コアジャーニー）

**実行日**: 2026-06-08
**実行**: /flow:e2e（Playwright 1.60 + Chromium、ローカル headless = Class A）
**サーバ**: vite dev（実キーなし = ローカルゲスト owner、offline-first local-first）
**対象 spec**: `e2e/core-journey.spec.ts`

## 結果: 3 passed (0 failed)

| シナリオ | カバー対象 | 結果 |
|---|---|---|
| セット作成→アイテム→実行→達成（UC1/2/4） | activity-sets, execution | ✅ |
| 継続サマリに達成反映（穴あき許容、UC6） | streak-summary, execution | ✅ |
| 法務ルート到達性 O55 + O54 文言 | _shared/legal, app-shell | ✅ |

## 検証内容
- 入口リード（O41）が表示される
- 匿名/キーなしでも全 local-first ジャーニーが実ブラウザで通る（offline degrade）
- 実行状態機械（開始→次へ→セット終了→done）が UI で動作
- 穴あき達成（1 アイテム実行）が継続サマリに反映、currentStreak 表示
- 法務リンク（フッタ常設、O55）から /legal/privacy に到達、O54 ゲスト削除文言を確認

## このレビューで発見・修正した実 gap
1. **App ルーティング欠落**: ExecutionPage / SetEditPage が未配線で実行フローに到達不能 → /sets/:id, /run/:setId, /summary/:setId を追加（E2E 準備で発見）。
2. **ClerkProvider が空キーで throw**: 本番キー未到達/オフラインで white-screen → owner 解決を OwnerContext に分離、キーなしでローカルゲスト動作（offline-first 実現）。
3. **実行後のサマリ導線欠落**: ExecutionPage done 状態から継続サマリへ行けない → RunInner に「継続を見る」導線追加。

## 実キー必要（Release gate 後の E2E、本ローカル headless では未カバー）
- tip-jar の実 Stripe Checkout フロー（実キー、実課金=Class B-4）
- サーバ同期（/api/sync push/pull、実 Neon/Clerk）
- 実 Clerk Google リンク（段階認証）
→ これらは `/flow:release` で実キー FILL 後、preview/実機で確認（P4.7）。
