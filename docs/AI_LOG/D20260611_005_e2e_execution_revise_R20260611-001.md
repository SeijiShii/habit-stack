# AI_LOG — /flow:e2e execution R20260611-001（計時状態の永続化・復帰）

- **実行日時**: 2026-06-11（JST）
- **コマンド**: /flow:e2e（/flow:auto P4.5 E2E gate 経由）
- **対象**: execution R20260611-001（revise_R20260611-001_20260611_timing-persistence-resume）
- **実行者**: seiji + Claude
- **状態**: 完了
- **含まれる decision 範囲**: D20260611-025〜027
- **テスト結果**: E2E 6/6 green（timing-persistence 3 + core-journey 3）、unit 167 green、typecheck clean
- **生成アーティファクト**: `e2e/timing-persistence.spec.ts`（新規）+ `103_REVISE_E2E_REPORT.md` + StrictMode 復元バグ修正（useExecution.ts）+ core-journey 文言ドリフト修正

## 主要決定サマリ
| decision_id | テーマ | chosen | type |
|---|---|---|---|
| D20260611-025 | StrictMode 復元バグ | restoredRef 同期 gate を廃し appliedRef を async 内（cancelled 後）で立てる方式へ。E2E が検出した本番致命バグ | auto-recommended |
| D20260611-026 | E2E スコープ | リロード復元 / ログイン終了 / サマリ継続の 3 本を実装。4H 放置/キャップ/15秒 push は時刻依存のため unit 委譲 | auto-recommended |
| D20260611-027 | core-journey ドリフト | 完了文言アサート（やれました→できました）を wording 校正済みに追従 | auto-recommended |

## 依存関係
- depends_on: revise/spec-review/tdd D20260611-001〜022（全 R1〜R8）

## Decisions

```yaml
- id: D20260611-025
  timestamp: 2026-06-11T19:48:00+09:00
  command: /flow:e2e
  phase: E2E 実行→バグ検出→修正
  question: リロード後に計時セッションが復元されない（E-RESUME/E-SUMMARY 赤）原因と修正
  chosen: useExecution の restoredRef 同期 gate を廃止し、appliedRef を async 内（cancelled 判定の後）で立てる方式へ変更
  chosen_type: auto-recommended
  depends_on: [D20260611-012]
  context: |
    IndexedDB には status=running セッションが正しく永続（owner 安定）されているのに、StrictMode 下で
    マウント復元 effect が早期 return し setState(found.state) が呼ばれない致命バグ。
    旧実装は restoredRef を effect 冒頭で同期的に true 化 → 一度目マウントの cleanup が cancelled=true →
    二度目（実）マウントは restoredRef=true で早期 return。結果どちらの async も適用しない。
    unit は StrictMode 無し render のため緑のまま本番バグを隠蔽（P83 StrictMode×storage の実発現）。
    修正: appliedRef を async 内 cancelled 後で立てる（一度目は cancelled で中断、二度目が 1 度だけ適用）。
    decideRecovery は純関数 / finalize は冪等 put のため二重実行に安全。E2E 6/6・unit 167・typecheck 全緑で確認。

- id: D20260611-026
  timestamp: 2026-06-11T19:30:00+09:00
  command: /flow:e2e
  phase: Step スコープ決定
  question: どの 004 シナリオを E2E 実装し、どれを unit へ委譲するか
  chosen: リロード復元(E-RESUME)/ログイン終了(E-LOGIN)/サマリ継続(E-SUMMARY) の 3 本を実装、4H放置・キャップ・15秒push は unit 委譲
  chosen_type: auto-recommended
  depends_on: []
  context: |
    4H 放置自動終了 / 4H キャップ / 15秒 backend push は決定的な now 注入が必須で実ブラウザの実時間では検証不能。
    recovery.test.ts / elapsed.test.ts / ExecutionPage.test.tsx（now・tick 注入）で網羅済み（004 §4 に整合）。
    E2E は実ブラウザで堅牢なジャーニー（リロード/ナビゲーション）に集中。

- id: D20260611-027
  timestamp: 2026-06-11T19:52:00+09:00
  command: /flow:e2e
  phase: リグレッション確認
  question: core-journey.spec.ts の完了文言アサート（やれました）が赤
  chosen: アサートを「できました」の部分一致へ更新（wording 校正 commit 24527d5 で軟化済み、テスト未追従の既存ドリフト）
  chosen_type: auto-recommended
  depends_on: []
  context: |
    本改修と無関係の既存ドリフト。ExecutionPage の done 文言は「おつかれさまでした。今日もひとつ、できました。」へ
    軟化済みだが E2E アサートが旧文言のまま。スコープ内同一機能の赤を放置しないため追従修正。
```
