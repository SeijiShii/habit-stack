# streak-summary E2E テスト計画（振り返り総覧 + 連続日数の正確化）

> **入力**: `./001_REVISE_SPEC.md`, `../../concept.md` §1.1, 既存 `../004_streak-summary_E2E_TEST.md`
> **最終更新**: 2026-06-13

---

## 1. 変更 UC シナリオ

### UC6-OV: 振り返り総覧
| シナリオ ID | 前提 | 操作ステップ | 期待結果 |
|---|---|---|---|
| E2E-OV-01 | セット 2 件・片方に実行記録あり | ナビ「継続」をクリック | `/summary` に総覧表示。ドロップダウン + セット 2 件の details が見える（行き止まりにならない） |
| E2E-OV-02 | 同上 | ドロップダウンでセット A を選択 | `/summary/:setIdA` に遷移し個別サマリ表示 |
| E2E-OV-03 | 同上 | セット A の details を開閉 | 開: アイテム名 + 累計時間が表示 / 閉: ヘッダのセット名 + 合計時間のみ |
| E2E-OV-04 | セット 0 件 | `/summary` を開く | 空状態メッセージ + セット作成導線 |

### UC6 streak（是正）
| シナリオ ID | 前提 | 操作ステップ | 期待結果 |
|---|---|---|---|
| E2E-ST-01 | 昨日・一昨日に達成記録（ローカル日付で注入） | 今日 `/summary/:setId` を開く（今日未実施） | 「2日つづいています」（0 日にならない） |
| E2E-ST-02 | E2E-ST-01 の状態から今日 1 アイテム実行 | サマリ再表示 | 「3日つづいています」 |

## 2. リグレッションシナリオ（既存 UC、重要度高）

| UC | シナリオ ID | 確認観点 |
|---|---|---|
| UC6 個別サマリ | E2E-RG-01 | `/summary/:setId` の期間切替（7日/30日）と達成ドット表示が従来通り |
| UC6 空状態 | E2E-RG-02 | 記録なしセットで「まだ記録がありません」中立表現 |
| UC4 実行フロー | E2E-RG-03 | 実行→終了→「継続を見る」で当日達成が反映（sessionLocalId のローカル日付化で壊れないこと） |

## 3. 移行検証シナリオ

| シナリオ ID | 移行前データ | 移行後期待状態 |
|---|---|---|
| E2E-MG-01 | UTC 日付でズレた daily_achievement + 正しい ISO の execution_record（IndexedDB へ直接注入） | 初回ロード後、サマリの達成ドット・streak がローカル日付基準で正しく表示。リロード 2 回目で変化なし（冪等） |

## 4. 環境要件差分

| 項目 | 前回 | 今回 | 理由 |
|---|---|---|---|
| TZ | 未指定 | headless ブラウザの TZ を `Asia/Tokyo` 固定 | UTC ズレの再現・検証 |

## 5. 期待 KPI

| 指標 | 目標 |
|---|---|
| 変更 UC シナリオ | 全 green |
| リグレッション | 全 green（既存 103 レポートの green 維持） |

## 6. 更新履歴
| 日付 | 変更概要 | 実行者 |
|---|---|---|
| 2026-06-13 | 初版作成 | /flow:revise |
