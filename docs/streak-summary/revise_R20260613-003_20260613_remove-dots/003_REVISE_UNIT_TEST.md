# streak-summary 単体テスト計画（達成日ドット表示の廃止）

> **入力**: `./001_REVISE_SPEC.md`, `./002_REVISE_PLAN.md`, `model/summarize.test.ts`
> **最終更新**: 2026-06-13

---

## 1. 追加テストケース

### 1.1 正常系
| ID | 対象 | 入力 | 期待出力 |
|---|---|---|---|
| U1 | `summarize` | 達成/未達混在の記録 | `dots` プロパティを返さない（型・実体ともに無い） |
| U2 | `SummaryPage` | 記録ありで描画 | `aria-label="達成日"` のドット列（`<ul>`）が DOM に存在しない |
| U3 | `SummaryPage` | 記録ありで描画 | 継続率（RateGauge）と「N日つづいています」テキストは存在する |

### 1.2 異常系
| ID | 対象 | 失敗条件 | 期待振る舞い |
|---|---|---|---|
| U4 | `SummaryPage` | 記録ゼロ | 「まだ記録がありません。ひとつから始めましょう。」を表示（ドット無し、従来どおり） |

### 1.3 境界値
| ID | 対象 | 境界 | 期待振る舞い |
|---|---|---|---|
| U5 | `summarize` | 全日達成 / 全日未達 | `achievedDays`/`currentStreak`/`rate` がドット廃止前と同値（回帰防止） |

## 2. 修正テストケース

| ID | 対象 | 修正前 | 修正後 | 理由 |
|---|---|---|---|---|
| M1 | `summarize.test.ts` L55 付近 | `s.dots.find((d) => d.date === "2026-06-09")?.achieved` を検証 | 同日の達成状態は `achievedDays`/`rate` の集計値で検証（or 当該アサーション削除） | `dots` を返さなくなるため |

## 3. 削除テストケース
| ID | 対象 | 削除理由 |
|---|---|---|
| D1 | `dots` 配列の要素検証（`AchievementDots` 描画前提のもの） | ドット表示廃止に伴い対象消滅 |

## 4. リグレッション強化
- `achievedDays` / `totalDays` / `currentStreak` / `rate` / `isEmpty` の算出が従来値と一致することを既存ケースで担保。
- 空状態メッセージ・期間切替（7日/30日）・ShareButton は不変であることを確認。

## 5. Mock 方針差分
| 対象 | 前回 | 今回 | 理由 |
|---|---|---|---|
| （なし） | — | — | repo mock は従来どおり |

## 6. カバレッジ目標
| 種別 | 目標 | 根拠 |
|---|---|---|
| 行 | 80% | 既存継承 |
| 分岐 | 70% | 既存継承 |

## 7. 更新履歴
| 日付 | 変更概要 | 実行者 |
|---|---|---|
| 2026-06-13 | 初版作成 | /flow:revise |
