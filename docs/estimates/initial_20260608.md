---
generated_at: 2026-06-08 16:35
generator: /flow:estimate
context: whole
source_input: ./docs/concept.md
interpreted_as:
  feature: null
  phase: refined
  source: autodetect
phase: refined
confidence_band:
  ai_impl: "±100%"
  human_bottleneck: "±50%"
  ai_tokens: "±30% (設計) / ±50% (実装込み)"
nfr_profile:
  scale: low
  throughput: low
  latency: standard
  availability: 99
  source: explicit
calibration:
  global_metrics: 0 セッション (global-metrics.jsonl 空、デフォルト係数)
  pj_stats: なし (グローバル100%、本見積もりが初回ベースライン)
  note: 実績蓄積後に refined_<date>.md (Phase 2 最初の feature 完了直後) で再キャリブレ
summary:
  min:
    total_files: 40
    total_lines: 3800
    human_hours: 3
    ai_tokens_design_k: 90
    ai_tokens_impl_k: 130
  standard:
    total_files: 76
    total_lines: 7800
    human_hours: 6
    ai_tokens_design_k: 150
    ai_tokens_impl_k: 300
  full:
    total_files: 150
    total_lines: 16500
    human_hours: 12
    ai_tokens_design_k: 280
    ai_tokens_impl_k: 640
---

# 見積もり: つみあげルーティン (habit-stack) 全体（初回ベースライン）

## サマリ表

| スコープ | ファイル数 | コード行数 (logic+test) | 人間時間 | AI 推論トークン (設計+実装) |
|---|---|---|---|---|
| Minimum | 40 | 3,800 | 3h | 90K + 130K = 220K |
| Standard | 76 | 7,800 | 6h | 150K + 300K = 450K |
| Full | 150 | 16,500 | 12h | 280K + 640K = 920K |

> **confidence band**: AI-impl ±100% (phase=refined) / Human-bottleneck ±50% / AI 推論トークン ±30%(設計)・±50%(実装込み)
> **NFR プロファイル**: scale=low, throughput=low, latency=standard, availability=99（個人ツール無料枠、§3 NFR・§4.4 由来）
> **金額換算**: コード行数 × AI コーディングレート、トークン × モデル単価（その時期で要確認）で利用側が算出
> **本見積もりの位置づけ**: 初回ベースライン。実績ゼロのため confidence 広め。Phase 2 で最初の feature 実装完了後に `refined_<date>.md` で再キャリブレ。

## 1. 機能フォルダ別ブレークダウン（Standard 値）

| フォルダ | 主タスク | Std files | Std lines(logic+test) | classification | human |
|---|---|---|---|---|---|
| activity-sets | セット/アイテム CRUD・並べ替え・時間帯 | 8 | 700+300 | ai_impl | — |
| execution | 時間ベース実行(開始/終了/一時停止/再開)・タイムスタンプ・今日メモ | 10 | 900+400 | ai_impl | — |
| streak-summary | 達成日判定・継続率・期間可視化(Recharts) | 7 | 600+250 | ai_impl | — |
| tip-jar | Stripe 単発・webhook 署名検証・非ブロッキング UI | 6 | 450+200 | ai_impl | tip 正常系実機 ~0.5h |
| feedback | ウィジェット + PII scrub + hub POST | 5 | 350+150 | ai_impl | — |
| **小計** | | **36** | **3,000+1,300** | | **~0.5h** |

**根拠**: execution が最重（実行状態機械 + タイムスタンプ算出 + local-first 書込 + 同期）。streak-summary は達成判定ロジック（セット単位・穴あき許容）+ Recharts。E2E は Playwright 自動化基本（O33）、人力は tip-jar 課金正常系の実機確認のみ計上。

## 2. 横断フォルダ別ブレークダウン（Standard 値、全て is_new=true）

| フォルダ | 主タスク | Std files | Std lines | is_new |
|---|---|---|---|---|
| _shared/db | Neon+Drizzle スキーマ 6 テーブル + マイグレーション | 4 | 300+50 | true |
| _shared/types | エンティティ型・enum(時間帯/実行状態) | 3 | 150+0 | true |
| _shared/auth | Clerk 匿名ゲスト→段階認証 + owner resolver + ゲスト→アカウント移行 | 5 | 450+250 | true |
| _shared/local-sync | IndexedDB + 同期キュー + 競合解決(last-write-wins) | 6 | 550+300 | true |
| _shared/legal | プラポリ/利用規約/特商法 静的 + consent | 4 | 250+50 | true |
| _shared/app-shell | 合成ルート + router + providers + API ルートハンドラ + PWA + deploy scaffold (O57) | 8 | 600+150 | true |
| **小計** | | **30** | **2,300+800** | |

**根拠**: _shared/auth は SEC-001(owner-check)/SEC-004(PII)を含む基盤（典型 認証基盤 Std 500/300）。_shared/local-sync は本 PJ 固有の重み（タイムスタンプ方式 + オフライン同期キュー）。app-shell は全部品の合成 target（O57、優先度最後）。

## 3. 基本部分（新規 PJ、Standard 値）

| 項目 | Std files | Std lines | 備考 |
|---|---|---|---|
| プロジェクト初期化(Vite+TS+PWA) | 3 | 150 | vite.config / PWA manifest / tsconfig |
| CI/CD(GitHub Actions + Vercel) | 2 | 120 | lint/typecheck/test + preview deploy |
| 環境変数/秘密管理(.env.example, gitignore済) | 1 | 40 | SEC-003 |
| dev スクリプト(scripts/dev.sh, stop.sh) | 2 | 90 | O36 |
| 監視(Sentry beforeSend + Vercel Analytics) | 2 | 100 | SEC-004 |
| **小計** | **10** | **500** | |

## 4. NFR 倍率の効き

| NFR 軸 | 値 | 倍率 |
|---|---|---|
| scale | low | 0.7x |
| throughput | low | 0.8x |
| latency | standard | 1.0x |
| availability | 99 | 1.0x |

合計 NFR 倍率: 約 0.56x（個人ツール無料枠のため軽い）。上表 Std 値は既にこのプロファイルを織り込み済み（汎用標準典型値から圧縮済み）。

## 5. 根拠サマリ

1. **execution が最大の実装ボリューム** — 実行状態機械 + タイムスタンプ差分算出 + local-first 書込が交差。タイマーを生で回さない設計（D20260608-004）でテストは決定的になり test 行数は抑えられる。
2. **_shared/local-sync が本 PJ 固有の重み** — IndexedDB + 同期キュー + 競合解決。time-budget sibling だが同期層は再実装。
3. **セキュリティは要件先行（SEC-001..005 accepted-as-requirement）** — owner resolver / Zod / PII scrub / webhook 署名は _shared/auth + 各 feature に織り込み済みとして計上（追加バッファ不要）。
4. **人間時間が小さい** — E2E 自動化基本（O33）。人力は tip-jar 課金正常系の実機確認 + 法務文面の最終確認 + 視覚デザインレビューの人間判断部分のみ（Std 合計 ~6h）。
5. **app-shell は優先度最後（O57）** — 全部品の合成 target。これが無いと「部品はあるが動くアプリが無い」状態が release まで露見するため必ず計上。

## 6. AI 推論トークン内訳（設計フェーズ、Std）

| セッション | 回数 | total/回(K) | 小計(K) |
|---|---|---|---|
| concept NEW | 1 | 13 | 13（実績） |
| secure design | 1 | 11 | 11（実績） |
| feature | 5 | 10 | 50 |
| spec-review | 5 | 8 | 40 |
| design | 1 | 15 | 15 |
| estimate | 2 | 8 | 16 |
| audit/その他 | — | — | ~5 |
| **設計合計** | | | **~150K** |

実装フェーズ: 約 7,800 lines × tokens/line × loop_factor（context 再読込込み）≈ 300K（±50%）。

## 7. 注意点

- confidence band が広い（±100%）のは初回ベースライン（実績ゼロ）のため。Phase 2 で最初の feature 実装後に metrics が蓄積され、`refined_<date>.md` で収束する。
- 全横断は is_new=true（新規 PJ、二重計上なし）。
- 金額換算は本書では行わない（コード量/トークン量が主指標）。
