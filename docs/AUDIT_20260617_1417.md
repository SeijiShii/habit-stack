# 設計書類整合性監査レポート

**監査日時**: 2026-06-17 14:17 (+09:00)
**監査実施者**: Claude (Opus 4.8) + seiji
**対象**: habit-stack（習慣スタック）
**scope**: standard（#1 構造 + #2 依存 + #3 論点 + #4 観点反映 + #5 AI_LOG + #6 PREREQUISITES）
**実行カテゴリ**: #1, #2, #3, #4, #5, #6
**前回監査との差分**: AUDIT_20260616_0833（full, C0/H0）以降 = guest-auth 全面書換（C20260617-001 Phase1-6、self-signed JWT 永続化で owner churn 根治）+ fix 設計/tdd。`/flow:auto` §3.0c 鮮度ゲート（大型 commit = fix 完遂 + auth 機構置換）由来の release-bound 監査。

## 1. サマリ

- **総検出問題**: 1 件
- **Critical**: 0 件
- **High**: 0 件
- **Medium**: 1 件（SCENARIO §5 カーソル drift）
- **Low**: 0 件
- **Info**: 0 件
- **§8 自動登録**: 0 件（`--auto-register-issues` 未指定）
- **未実装カテゴリ**: #7 収益 / #8 テストカバレッジ / #9 法務（full スコープ、本回 standard のため未実行）
- **抑制された検出**: 0 件（`.audit-suppressions.yml` 不在）
- **履歴トレンド方向**: 横ばい（Critical/High = 0 を維持、直近 8 回連続 C0/H0）
- **常習化検出**: 0 件

### ⭐ 本回の主眼: O22 step 3.9（owner churn / データ保存不変条件）= PASS 化を確認
CF-20260617-001 で新設された #4 step 3.9（guest identity 永続性 / owner churn 検出）に対し、C20260617-001 の guest-JWT 永続化実装が **PASS** を満たすことを確認した（詳細 §3.4）。本バグの再発防止 perspective が今や緑であることを監査として裏付けた。

## 2. カテゴリ別サマリ

| # | カテゴリ | 実行状態 | 検出件数 (C/H/M/L) | 主要問題 |
|---|---|---|---|---|
| #1 | 構造的整合性 | ✅ 実行 | 0 / 0 / 0 / 0 | 機能・横断フォルダと INDEX 一致、参照切れなし |
| #2 | 依存整合性 | ✅ 実行 | 0 / 0 / 0 / 0 | 循環依存なし。env-coverage 完全（`GUEST_TOKEN_SECRET` 新規も .env.example 記載済）、placeholder fallback なし |
| #3 | 論点ステータス | ✅ 実行 | 0 / 0 / 0 / 0 | open 論点（002/003/009/010/011）すべて正当（accepted-risk or feedback-hub 待ち）、期限超過なし |
| #4 | 観点反映 | ✅ 実行 | 0 / 0 / 0 / 0 | applicable require 観点 全 PASS（§3.4 完全列挙テーブル）。**O22(D) owner churn = FAIL→PASS** |
| #5 | AI_LOG 整合性 | ✅ 実行 | 0 / 0 / 0 / 0 | INDEX が D20260617_001-003 を反映、状態=完了・Decisions 充足、depends_on 解決可 |
| #6 | PREREQUISITES 整合性 | ✅ 実行 | 0 / 0 / 1 / 0 | （SCENARIO §5 drift は #3/横断で 1 件、§3.3） |
| #7 | 収益モデル整合性 | ⏳ 未実行 | - | full スコープ |
| #8 | テスト計画カバレッジ | ⏳ 未実行 | - | full スコープ |
| #9 | 法務書類整合性 | ⏳ 未実行 | - | full スコープ |

## 3. 詳細検出結果

### 3.1 Critical
なし。

### 3.2 High
なし。

### 3.3 Medium
#### [AUDIT-issue-001] SCENARIO §5 カーソルが stale（2026-06-13 時点で固定）
- **カテゴリ**: #3 論点ステータス / 横断整合
- **検出根拠**: `docs/SCENARIO.md` §5 現在地カーソルが「最終更新 2026-06-13 11:20」で、以降の以下を反映していない:
  - C20260616-001（set-data-loss-after-login）fix の本番デプロイ（commit 124adc2, smoke green）
  - C20260617-001（token-stale-owner-churn）guest-auth 全面書換（Phase1-6 + tdd、unit 272 green、未デプロイ）
  - §5 は依然「未デプロイ改修 = revise R20260613-001」を現在地としており、実 HEAD と乖離
- **推奨修正アクション**: `/flow:scenario --update`（§5 を HEAD 同期: C20260616-001 デプロイ済 + C20260617-001 実装済・未デプロイを反映、最終更新時刻更新）
- **§8 登録**: 不要（bookkeeping drift、論点化せず scenario update で解消）

### 3.4 #4 観点反映 — require 観点 完全列挙テーブル（forcing function、CF-20260613-001）

`perspectives.md` を ID 順走査し、habit-stack（local-first 習慣トラッカー / guest+Google progressive auth / tip-jar 投げ銭 / public-facing givers.work / service-hub メンバー）に applicable な require/detect-gated 観点を全列挙:

| 観点 | applicable 判定 | verdict | 根拠（signal） |
|---|---|---|---|
| O12 プラポリ/データ主体権利 | require（公開 + PII） | ✅ PASS | プラポリ + セルフ削除（`selfDelete`/`dataOps.deleteAll`）実装 |
| O22(A) 匿名ゲスト | require（guest 宣言） | ✅ PASS | `guestProvision`/`guestClient`/`AuthProvider` ゲスト経路 |
| O22(B) 段階的認証/ログイン動線 | require（progressive auth） | ✅ PASS | `signInWithGoogle`+`authenticateWithRedirect`（AuthProvider.tsx:144,146） |
| O22(C) step-up/reverification (3.8) | detect-gated（外部連携+guest） | ✅ PASS | guest=自前署名 JWT（非 Clerk session）化により Google 連携時は fresh Clerk session 生成 + `reassignOwner`（dataOps.ts:37）で owner 引継ぎ。aged-guest 403 詰みの構造を解消 |
| **O22(D) owner churn / データ保存不変条件 (3.9)** | detect-gated（owner-scoped local-first + guest） | ✅ **PASS（FAIL→PASS）** | guest identity 永続 signal **存在**: `signGuestToken`/`verifyGuestToken`/`getStoredGuestToken`/`storeGuestToken`/`GUEST_TOKEN_KEY`（guestToken.ts/guestClient.ts）。旧アンチパターン（Clerk `strategy:'ticket'`/`createSignInToken`）= **0 件（撤去済）**。owner 移行は非破壊 `reassignOwnerLocal`（localStore.ts:179）。= C20260617-001 が根治した当該バグの perspective が緑 |
| O31 製品内グロース/シェア導線 | require（公開） | ✅ PASS | `ShareButton`（streak-summary）+ `navigator.share` 系 |
| O39 デザイン SoT | require（UI） | ✅ PASS | design-system 適用済（視覚レビュー green、prior） |
| O40 フィードバック導線 | require | ✅ PASS | `api/feedback.ts` + feedback-hub 連携 |
| O48 service-info（producer 契約） | require（hub メンバー） | ⚠️ 不変（§注） | producer endpoint `api/hub/service-info` は本回も不在。**guest-auth 変更は O48 を一切触らない**ため AUDIT_20260616_0833（full, C0/H0）の判定を踏襲（regression なし）。本監査の release-pre full（次反復）で再確認対象 |
| O54 DSR セルフ削除 | require（guest + 法務） | ✅ PASS | `selfDelete`/`dataOps`、本番 DELETE /api/account 401 で履行可能（prior smoke） |
| O55 ルート到達性 | require | ✅ PASS | 全 route 到達可（prior） |
| O56 favicon/PWA アイコン | require（公開） | ✅ PASS | index.html wiring + `public/favicon.svg`/`apple-touch-icon.png`/`icon-192/512.png` 実在 + VitePWA で manifest 生成（wired-but-missing なし） |
| O62 showcase back-link（他アプリ誘導） | require（公開 maker） | ✅ PASS | `src/config/showcase.ts` + `LegalFooter` で givers.work showcase 誘導 |
| O64 webhook 3-state 検証 | detect-gated（投げ銭 + payment webhook 実在） | ✅ PASS | `api/tip/webhook.ts` に `constructEvent`/`stripe-signature` |

> **§注 O48**: HUB_SERVICE_INFO_SECRET は `.env.example` + `api/feedback.ts` に存在するが、producer の `/api/hub/service-info` 専用 endpoint は未実装。本件は guest-auth 変更と独立で、直近 full 監査（C0/H0）で High 化されていない（habit-stack の hub 登録方針として現状許容 or 別経路）。**standard 監査では既往判定を踏襲し新規 finding にしない**。次反復の release-pre `--scope=full` で O48 を改めて精査する。

**#4 完了条件**: applicable require 観点が全行 verdict 付きで列挙済 → #4 = 新規 0 件（O22(D) は FAIL→PASS の改善）。

## 4. 推奨アクション一覧

| # | severity | アクション | 実行コマンド |
|---|---|---|---|
| 1 | Medium | SCENARIO §5 を HEAD 同期（C20260616-001 デプロイ済 + C20260617-001 実装済・未デプロイ） | `/flow:scenario --update` |
| 2 | - | （release-pre）guest-auth 書換の本番反映前に full 監査 + secure | `/flow:audit --scope=full` → `/flow:secure` |

## 5. 次回監査推奨タイミング

- **次反復（release-pre 必須監査）**: C20260617-001 を本番デプロイする前に `--scope=full` + secure を 1 回通す（§3.0c ハードゲート、最新 AUDIT 参照 commit = HEAD でないため）。
- リリース後: light で軽量チェック。

## 6. 監査履歴（直近 8 件）+ トレンド

| 日時 | scope | C | H | M | L | コメント |
|---|---|---|---|---|---|---|
| 2026-06-17 14:17（本回） | standard | 0 | 0 | 1 | 0 | guest-auth 書換後、O22(D) owner churn = PASS 確認 |
| 2026-06-16 08:33 | full | 0 | 0 | - | - | C20260616-001 release-pre |
| 2026-06-15 09:40 | standard | 0 | 0 | - | - | |
| 2026-06-14 19:12 | standard | 0 | 0 | - | - | |
| 2026-06-14 08:40 | standard | 0 | 0 | - | - | aged-guest 403（O22 C） |
| 2026-06-13 18:50 | light | 0 | 0 | - | - | O62 反映 |
| 2026-06-13 18:40 | full | 0 | 0 | - | - | |
| 2026-06-11 20:25 | full | 0 | 0 | - | - | release-pre |

### 6.2 トレンド判定
- **方向**: 横ばい（C/H = 0 維持、8 回連続）
- **常習化検出**: なし

## 7. 抑制サマリ
`.audit-suppressions.yml` 不在 → 抑制 0 件。
