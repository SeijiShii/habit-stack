# 設計書類整合性監査レポート

**監査日時**: 2026-06-11 20:00 (+09:00)
**監査実施者**: Claude (claude-opus-4-8) + seiji
**対象**: habit-stack（プロダクト全体）
**scope**: standard（#1 構造 + #2 依存 + #3 論点 + #4 観点反映 + #5 AI_LOG + #6 PREREQUISITES）
**実行カテゴリ**: #1, #2, #3, #4, #5, #6
**前回監査**: AUDIT_20260609_1851.md（light、Critical 0 / High 1 [O31] / Low 2）。以降 **28 commits**（revise R20260610-001・fix C20260610-001・revise R20260611-001 の大型完遂を含む）= §3.0c 鮮度トリガ超過のため /flow:auto が自動実行。

## 1. サマリ

- **総検出問題**: 4 件
- **Critical**: 1 件（詳細 §3.1）
- **High**: 0 件
- **Medium**: 0 件
- **Low**: 3 件
- **Info**: 0 件
- **§8 自動登録**: 0 件（--auto-register-issues 未指定 → 登録推奨を注記）
- **未実装カテゴリ**: #7 収益 / #8 テストカバレッジ / #9 法務（full のみ、本回 standard では枠組みのみ）
- **抑制された検出**: 0 件
- **履歴トレンド方向**: 改善（前回 High 1 [O31] が実装で解消）+ **新規 Critical 1 検出**（standard 昇格で #4 step 5.5 O12×O22 ペア検査が初稼働）
- **常習化検出**: 0 件

## 2. カテゴリ別サマリ

| # | カテゴリ | 実行 | 検出 (C/H/M/L) | 主要問題 |
|---|---|---|---|---|
| #1 | 構造的整合性 | ✅ | 0/0/0/1 | AI_LOG INDEX が 59 セッション中 13 のみ列挙（総数表記 13、実体 59）(Low) |
| #2 | 依存整合性 | ✅ | 0/0/0/0 | 循環なし / env-coverage 充足 / placeholder fallback なし |
| #3 | 論点ステータス | ✅ | 0/0/0/2 | 論点-001（解消済・§7 未移動）/ 論点-002（サブドメ確定済・§8 open 残存）(Low×2) |
| #4 | 観点反映 | ✅ | 1/0/0/0 | **O54/O12×O22: プラポリ+利規が「アプリ内セルフ削除」を約束も UI 導線が皆無（Critical）** |
| #5 | AI_LOG 整合性 | ✅ | (—#1 と同根) | decision chain 健全、INDEX セッション表のみ stale |
| #6 | PREREQUISITES 整合性 | ✅ | 0/0/0/0 | PREREQUISITES.md 実在、主要節充足 |

## 3. 詳細検出結果（severity 降順）

### 3.1 Critical

#### [AUDIT-perspective-001] O54/O12×O22: アプリ内セルフサービス削除の UI 導線が未実装（約束済み・履行不能）
- **カテゴリ**: #4 観点反映（step 5.5 O12×O22 ペア検査）
- **検出根拠**:
  - プラポリ本文（`src/features/legal/content.ts:25`）が「データの確認・削除はアプリ内のセルフサービス機能でご自身で行えます（**全データ削除はいつでも実行できます**）」と明記。
  - 利用規約（同 `:41`）も「解約: **アプリ内の全データ削除**によりいつでも利用を終了できます」と明記。
  - 削除ロジック `deleteAllData`（`src/services/auth/dataOps.ts:26`）+ `LocalStore.wipeOwner` は実装済み。
  - しかし **`deleteAllData` の呼び出し元が定義箇所とテスト以外に存在しない**（`grep -rn deleteAllData src/ | grep -v test` = 定義 1 行のみ）。`AccountPage.tsx` はサインアウト + 「Google で引き継ぐ」導線のみで、削除ボタン/確認 UI が無い。
  - = O22 ゲスト/匿名認証がスコープの PJ で、プラポリ+利規が「アプリ内セルフ削除」を約束しているのに **本番アプリにその UI 導線が無い**（運営は匿名ゲストを特定できず窓口削除も不能 = 履行手段ゼロ）。個情法/GDPR 消去権の不履行リスク。**本番公開済み**（habit-stack.givers.work）アプリでの法令ギャップ。
- **推奨修正アクション**: `/flow:revise _shared/auth`（または account 機能）で **AccountPage に「全データを削除」セルフサービス導線**（確認ダイアログ + `deleteAllData(db, ownerId)` 呼び出し + 完了後ローカル wipe/リロード）を実装。プラポリ N2 文言（content.ts）と整合。実装後 unit で「ボタン→deleteAllData 呼び出し」を検証、E2E に削除導線到達を追加。
- **§8 登録**: 登録推奨（--auto-register-issues 未指定のため未登録）。Critical につき即時対応推奨。

### 3.2 High
（なし。前回 High [O31 アプリ内シェア導線] は commit 1d326d5 で実装され解消 ✅）

### 3.3 Low
| rule_id | target | 内容 | 推奨アクション |
|---|---|---|---|
| AUDIT-structure-001 | docs/AI_LOG/INDEX.md | セッション表が実体 59 件中 13 件のみ列挙、総セッション数表記も不一致 | AI_LOG INDEX 再生成（bookkeeping、auto §3.0c で reconcile） |
| AUDIT-issue-001 | [論点-001] | ストリーク見せ方は D20260608-003 で解消済だが §8 に open 残存 | `/flow:concept` UPDATE で §7 へ移動 |
| AUDIT-issue-002 | [論点-002] | 公開ドメインは habit-stack.givers.work で確定済だが §8 に open 残存 | `/flow:concept` UPDATE で §7 へ移動 |

## 4. 推奨アクション一覧（severity 降順）

| # | severity | アクション | 実行コマンド |
|---|---|---|---|
| 1 | Critical | セルフサービス削除 UI 導線を実装（約束済み消去権の履行） | `/flow:revise _shared/auth`（AccountPage に削除導線 + deleteAllData 配線） |
| 2 | Low | AI_LOG INDEX 再生成 | auto §3.0c reconcile（bookkeeping） |
| 3 | Low | 論点-001/002 を §7 へ移動 | `/flow:concept` UPDATE |

## 5. 次回監査推奨タイミング

- **Critical [AUDIT-perspective-001] 解消後**: 即時再監査（standard）
- **再デプロイ前**: 未デプロイの R20260611-001（計時永続化）+ 本 Critical 修正をまとめて release-pre full audit → secure
- **月次**: standard

## 6. 監査履歴（直近 5 件）+ トレンド

### 6.1 履歴テーブル
| 日時 | scope | Critical | High | Medium | Low | コメント |
|---|---|---|---|---|---|---|
| 2026-06-11 20:00（本回） | standard | 1 | 0 | 0 | 3 | standard 昇格で #4 O12×O22 ペア検査が初稼働 → Critical 1 検出 |
| 2026-06-09 18:51 | light | 0 | 1 | 0 | 2 | #4 light 昇格で O31 シェア未実装 High 検出 |
| 2026-06-09 18:41 | light | 0 | 0 | 0 | 2 | — |
| 2026-06-08 | light | 0 | 0 | 0 | — | 初期 |

### 6.2 トレンド判定
- **方向**: 改善（O31 High 解消）だが **standard カテゴリ拡張で潜在 Critical 1 を新規 surface**。
- **判定根拠**: light（#1-#4）では O12×O22 ペア検査（#4 step 5.5）は走るが、今回 deleteAllData の UI 呼び出し元欠落を AccountPage 実装の進展後に初めて検出。前回 light は O31 にフォーカスし O54 導線欠落を拾えていなかった。
- **常習化検出**: なし。

### 6.3 改善ハイライト（前回比）
- 解消: [O31] アプリ内シェア導線（ShareButton.tsx 実装、commit 1d326d5）。
- 新規: [O54/O12×O22] セルフサービス削除 UI 欠落（Critical）。

## 7. 抑制サマリ
- 適用された抑制: 0 件（`.audit-suppressions.yml` 不在）。
- 新規追加: 0 件。
- 期限切れ復活: 0 件。
