# _shared/app-shell 単体テスト計画（アプリタイトル左ロゴ + 幅不足時ロゴのみ）

> **入力**: `./001_REVISE_SPEC.md`, `./002_REVISE_PLAN.md`, `src/app/App.test.tsx`
> **最終更新**: 2026-06-13

---

## 1. 追加テストケース

### 1.1 正常系
| ID | 対象 | 入力 | 期待出力 |
|---|---|---|---|
| U1 | `BrandLogo` | 既定 | `<svg>` が描画される |
| U2 | `BrandLogo` | `size={32}` | svg の width/height が 32 |
| U3 | `AppLayout` ヘッダ | 描画 | ホームリンク内に BrandLogo（svg）が存在 |
| U4 | `AppLayout` ヘッダ | 描画 | ホームリンクが `aria-label="つみあげルーティン（ホーム）"` を持つ |
| U5 | `AppLayout` ヘッダ | 描画 | タイトルテキスト「つみあげルーティン」が `.brand-name` 要素として存在（DOM 上は常に存在、視覚的縮退は CSS） |

### 1.2 異常系
| ID | 対象 | 失敗条件 | 期待振る舞い |
|---|---|---|---|
| U6 | `BrandLogo` | — | 装飾要素として `aria-hidden="true"` を持つ（スクリーンリーダに二重読みさせない） |

### 1.3 境界値
| ID | 対象 | 境界 | 期待振る舞い |
|---|---|---|---|
| U7 | 縮退挙動 | 狭幅 | CSS による縮退のため単体（jsdom）では検証せず E2E（viewport）で担保。本計画では DOM 構造の存在のみ確認 |

## 2. 修正テストケース

| ID | 対象 | 修正前 | 修正後 | 理由 |
|---|---|---|---|---|
| M1 | `App.test.tsx` のヘッダ参照 | テキスト「つみあげルーティン」一致でホームリンク取得 | リンクの role/aria-label 基準で取得 | タイトルが span 化＋狭幅で視覚的に隠れ得るため、取得手段を堅牢化 |

## 3. 削除テストケース
| ID | 対象 | 削除理由 |
|---|---|---|
| （なし） | — | — |

## 4. リグレッション強化
- 既存の app-shell テスト（nav リンク群 / 入口リード O41 / 法務フッタ）は全維持。ホームリンクの遷移先 `/` が不変であることを確認。

## 5. Mock 方針差分
| 対象 | 前回 | 今回 | 理由 |
|---|---|---|---|
| （なし） | — | — | 外部依存追加なし |

## 6. カバレッジ目標
| 種別 | 目標 | 根拠 |
|---|---|---|
| 行 | 80% | 既存継承 |
| 分岐 | 70% | 既存継承 |

## 7. 更新履歴
| 日付 | 変更概要 | 実行者 |
|---|---|---|
| 2026-06-13 | 初版作成 | /flow:revise |
