# クレーム調査レポート

**claim id**: C20260609-001
**実施日**: 2026-06-09
**対象機能**: _shared/app-shell（横断 UI / デザイン基盤）
**緊急度推定**: high（公開直後、第一印象を損なう）

## 1. クレーム原文

```
アプリ画面にスタイルが当たっていない
```

（本番 https://habit-stack.givers.work の実機スモーク中に報告）

## 2. 分解結果

### 2.1 期待挙動 (Expected)
design-system.md の世界観（穏やか・積み上げ）が画面に適用されている — カード（surface + radius-lg + shadow）、主ボタン（primary 塗り pill）、色・余白・タイポのトークンが反映されたデザイン済み UI。

### 2.2 現実挙動 (Actual)
素のセマンティック HTML 見た目。`theme.css` の要素セレクタ（body 背景 / button / main 中央寄せ）と CSS 変数（トークン定義）のみが効き、各コンポーネントにはデザインが当たっていない。リンクは既定のブラウザ青リンク、カードや主ボタンの塗りが無い。

### 2.3 発生条件
- 環境: 本番（および同一ビルドのためローカル preview でも同様）
- 全画面共通（HomePage / セット / 実行 / 振り返り 等）

### 2.4 影響範囲
- 全ユーザー・全画面。機能は動くが見た目が未完成。

### 2.5 報告経路
- 本人（seiji）による本番実機スモーク。温度感: 冷静（リリース検証の一環）。

### 2.6 報告者文脈
公開した MVP の第一印象を確認 → デザインが反映されていないことに気づいた。

## 3. 過去類似クレーム
該当なし（初回 claim）。
