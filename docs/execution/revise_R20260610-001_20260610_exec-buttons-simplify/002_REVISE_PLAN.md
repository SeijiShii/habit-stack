# execution 変更計画書（実行画面ボタンの簡素化）

> **入力**: `./001_REVISE_SPEC.md`, `src/features/execution/ExecutionPage.tsx`（+ テスト）
> **最終更新**: 2026-06-10

---

## 1. 既存ファイル変更一覧

| ファイル | 変更内容（概要） | リスク | 関連 SPEC § |
|---|---|---|---|
| `src/features/execution/ExecutionPage.tsx` | running ブロックから「終了」ボタン（onClick=exec.endItem）を削除 / 「次へ」→「次の活動へ」 / paused「次を開始」→「次の活動へ」 | 低（表示層のみ） | §2.1, §2.2 |
| `src/features/execution/ExecutionPage.test.tsx` | 既存テスト「開始→次へ→終了で完了」のボタン名 `次へ`→`次の活動へ` に修正。テスト名も実態に合わせて更新 | 低 | §003 |

> `hooks/useExecution.ts` の `endItem` メソッドは残置（nextItem/endSession が状態機械 endCurrentItem を内部利用するため）。UI から未参照になるだけ。`model/executionMachine.ts` は不変。

## 2. 新規ファイル一覧
なし。

## 3. 削除ファイル一覧
なし（ボタン要素の削除のみ、ファイル削除なし）。

## 4. マイグレーション要否
- DB スキーマ変更: ❌
- 既存データ変換: ❌
- 設定ファイル変更: ❌
- ストレージパス変更: ❌
→ **マイグレーション不要**（005 生成なし）。

## 5. 実装 Phase 分割（単一 Phase・軽）

### Phase 1 (RED→GREEN→IMPROVE)
- 対象: `ExecutionPage.tsx`（running ブロック）+ `ExecutionPage.test.tsx`
- ゴール:
  1. running ブロックに「終了」ボタンが存在しない。
  2. running の次ボタン文言が「次の活動へ」。
  3. paused の次ボタン文言が「次の活動へ」。
  4. 「次の活動へ」で次アイテムへ進む（達成記録は従来どおり）。
  5. 既存の達成記録・メモ・タイマー表示テストが維持される。

## 6. 依存関係順序
単一ファイル変更につき順序依存なし。

## 7. ロールアウト計画
| ステップ | 内容 | 期日 | 検証方法 |
|---|---|---|---|
| 1 | 実装 + 単体テスト green | 2026-06-10 | vitest |
| 2 | 本番リリース | 改修確定後 | /flow:release（live PJ、target は都度確認） |

## 8. リスク・注意点
- 「終了」ボタンに慣れたユーザーの操作変化（軽微）。代替動線（次の活動へ / セット終了）が明確なので問題なし。
- 最終アイテムでは「次の活動へ」非表示 → 完了は「セット終了」（従来踏襲、変更なし）。

## 9. 完了の定義 (DoD)
- [ ] 「終了」ボタンが running から消えている
- [ ] running/paused の次ボタンが「次の活動へ」に統一
- [ ] 「次の活動へ」で次アイテム遷移 + 達成記録が従来どおり
- [ ] 既存テスト（達成 / メモ / タイマー表示 R1-R7）維持
- [ ] 単体テスト green

## 10. 更新履歴
| 日付 | 変更概要 | 実行者 |
|---|---|---|
| 2026-06-10 | 初版作成 | /flow:revise |
