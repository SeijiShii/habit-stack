# execution 単体テスト計画（実行画面ボタンの簡素化）

> **入力**: `./001_REVISE_SPEC.md`, `./002_REVISE_PLAN.md`, `src/features/execution/ExecutionPage.test.tsx`
> **最終更新**: 2026-06-10
> **テスト基盤**: vitest + @testing-library/react + happy-dom

---

## 1. 追加テストケース

### 1.1 正常系
| ID | 対象 | 入力 | 期待出力 |
|---|---|---|---|
| U1 | running ボタン構成 | 開始 | 「終了」ボタンが**存在しない**（`queryByRole('button',{name:'終了'})` が null）。「次の活動へ」「一時停止」「セット終了」は存在 |
| U2 | 文言統一（running） | 開始 | running の次ボタン名が「次の活動へ」 |
| U3 | 文言統一（paused） | 開始 → 一時停止 | paused の次ボタン名が「次の活動へ」 |
| U4 | 「次の活動へ」で遷移 | 開始 → 次の活動へ | current-item が次アイテムに変わる |

### 1.2 異常系
該当なし（UI 文言/構成変更）。

### 1.3 境界値
| ID | 対象 | 境界 | 期待振る舞い |
|---|---|---|---|
| U5 | 最終アイテム | 最終アイテムで running | 「次の活動へ」は非表示（isLast）。「セット終了」で完了できる |

## 2. 修正テストケース

| ID | 対象 | 修正前 | 修正後 | 理由 |
|---|---|---|---|---|
| M1 | 既存「開始 → 次へ → 終了で完了 + 達成記録」 | `getByRole('button',{name:'次へ'})` | `getByRole('button',{name:'次の活動へ'})` + テスト名を「開始 → 次の活動へ → セット終了で完了 + 達成記録」に更新 | 文言統一に追従。達成記録のアサーションは維持 |

## 3. 削除テストケース
なし（「終了」ボタンをクリックする既存テストは存在しないため削除不要）。

## 4. リグレッション強化
- 既存維持: 「開始後にメモ入力できる」/ タイマー表示 R1・R5・R7・R3-R4 / 達成記録 / machine・repo テスト群（全て不変想定）。
- 追加チェック: U1（終了ボタン不在）で「削除されたまま」を回帰検知。

## 5. Mock 方針差分
変更なし（既存の fake-indexeddb / 注入 now を踏襲）。

## 6. カバレッジ目標
| 種別 | 目標 | 根拠 |
|---|---|---|
| 変更行（ボタン JSX） | 100% | 表示要素の存在/文言/onClick を直接アサート |

## 7. 更新履歴
| 日付 | 変更概要 | 実行者 |
|---|---|---|
| 2026-06-10 | 初版作成 | /flow:revise |
